# AiNote 设置界面设计方案

## 一、整体架构

### 1.1 导航位置

设置界面作为独立的一级菜单项，位于侧边栏底部:

```
┌─────────────────┐
│  📁 全部笔记    │
│  ⭐ 收藏        │
│  🗑️ 回收站      │
│                 │
│  ─────────────  │
│                 │
│  ⚙️  设置        │  ← 新增
└─────────────────┘
```

### 1.2 设置页面布局

采用 **左侧菜单 + 右侧内容** 的经典布局:

```
┌──────────────────────────────────────────────┐
│  设置                          [保存] [取消] │
│  ┌──────────┬─────────────────────────────┐  │
│  │ 账号     │                             │  │
│  │ 个人资料 │      内容区域                │  │
│  │ AI助手   │                             │  │
│  │ 模型配置 │                             │  │
│  │ 同步设置 │                             │  │
│  │ 外观     │                             │  │
│  │ 快捷键   │                             │  │
│  │ 关于     │                             │  │
│  └──────────┴─────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

---

## 二、菜单结构设计

### 2.1 一级菜单

| 菜单项 | 图标 | 说明 |
|--------|------|------|
| 账号 | UserOutlined | 账户信息、密码修改 |
| 个人资料 | IdcardOutlined | 用户资料、头像设置 |
| AI助手 | RobotOutlined | AI助手管理 |
| 模型配置 | ApiOutlined | 模型配置、API密钥 |
| 同步设置 | CloudSyncOutlined | 同步设置、冲突处理 |
| 外观 | BgColorsOutlined | 主题、字体、颜色 |
| 快捷键 | KeyboardOutlined | 快捷键配置 |
| 关于 | InfoCircleOutlined | 版本信息、帮助文档 |

### 2.2 菜单图标映射

```typescript
const settingMenuItems = [
  {
    key: 'account',
    icon: <UserOutlined />,
    label: '账号',
    path: '/settings/account'
  },
  {
    key: 'profile',
    icon: <IdcardOutlined />,
    label: '个人资料',
    path: '/settings/profile'
  },
  {
    key: 'ai-assistants',
    icon: <RobotOutlined />,
    label: 'AI助手',
    path: '/settings/ai-assistants'
  },
  {
    key: 'models',
    icon: <ApiOutlined />,
    label: '模型配置',
    path: '/settings/models'
  },
  {
    key: 'sync',
    icon: <CloudSyncOutlined />,
    label: '同步设置',
    path: '/settings/sync'
  },
  {
    key: 'appearance',
    icon: <BgColorsOutlined />,
    label: '外观',
    path: '/settings/appearance'
  },
  {
    key: 'shortcuts',
    icon: <KeyboardOutlined />,
    label: '快捷键',
    path: '/settings/shortcuts'
  },
  {
    key: 'about',
    icon: <InfoCircleOutlined />,
    label: '关于',
    path: '/settings/about'
  }
]
```

---

## 三、功能模块详细设计

### 3.1 账号设置

**路由**: `/settings/account`

**功能**:
- 显示当前登录用户信息
- 邮箱、用户名、显示名称
- 修改密码
- 登出账号

**界面元素**:
```tsx
<Form>
  <Form.Item label="邮箱">
    <Input disabled value={user.email} />
  </Form.Item>
  <Form.Item label="用户名">
    <Input value={user.username} />
  </Form.Item>
  <Form.Item label="显示名称">
    <Input value={user.displayName} />
  </Form.Item>
  <Form.Item label="修改密码">
    <Button>修改密码</Button>
  </Form.Item>
</Form>
```

---

### 3.2 AI助手管理

**路由**: `/settings/ai-assistants`

**功能**:
- 查看所有AI助手列表
- 创建自定义助手
- 编辑助手配置
- 删除自定义助手
- 内置助手不可删除

**界面布局**:
```
┌─────────────────────────────────────┐
│  [+ 新建助手]                       │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ ✍️  写作助手               │    │
│  │ 帮助你进行写作和内容创作    │    │
│  │ 内置助手 | gpt-3.5-turbo    │    │
│  │              [编辑]         │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 💻 代码助手               │    │
│  │ 代码生成、解释和调试        │    │
│  │ 内置助手 | gpt-3.5-turbo    │    │
│  │              [编辑]         │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

**数据库表**: `ai_assistants`

**API接口**:
- `GET /api/v1/ai/assistants` - 获取助手列表
- `POST /api/v1/ai/assistants` - 创建助手
- `PUT /api/v1/ai/assistants/:id` - 更新助手
- `DELETE /api/v1/ai/assistants/:id` - 删除助手

---

### 3.3 模型配置

**路由**: `/settings/models`

**功能**:
- 查看模型配置列表
- 添加新模型配置
- 编辑模型配置
- 删除模型配置
- 设置默认模型
- 查看使用统计

**界面布局**:
```
┌──────────────────────────────────────────┐
│  [+ 添加模型]                            │
├──────────────────────────────────────────┤
│  模型列表                                │
│  ┌────────────────────────────────┐      │
│  │ OpenAI GPT-3.5         ✓默认   │      │
│  │ API Key: sk-...****            │      │
│  │ 端点: https://api.openai.com   │      │
│  │ 已调用: 156次 | 成功率: 98.7%  │      │
│  │           [编辑] [禁用] [删除] │      │
│  └────────────────────────────────┘      │
│  ┌────────────────────────────────┐      │
│  │ Anthropic Claude       [禁用]  │      │
│  │ API Key: sk-...****            │      │
│  │ 端点: https://api.anthropic.com│      │
│  │ 已调用: 0次                    │      │
│  │           [编辑] [启用] [删除] │      │
│  └────────────────────────────────┘      │
└──────────────────────────────────────────┘
```

**数据库表**: `model_configs`, `model_usage_logs`

**API接口**:
- `GET /api/v1/models/configs` - 获取模型列表
- `POST /api/v1/models/configs` - 添加模型
- `PUT /api/v1/models/configs/:id` - 更新模型
- `DELETE /api/v1/models/configs/:id` - 删除模型
- `GET /api/v1/models/usage` - 获取使用统计
- `GET /api/v1/models/usage/logs` - 获取调用日志

**表单字段**:
- 模型名称
- 描述
- API Key (加密存储)
- API 端点
- 模型 ID
- 温度 (0-2)
- 最大 Token 数
- Top-P (0-1)
- 是否启用
- 是否默认

---

### 3.4 同步设置

**路由**: `/settings/sync`

**功能**:
- 查看同步状态
- 手动同步(推送/拉取)
- 查看同步冲突
- 解决冲突
- 同步设置(自动同步间隔等)

**界面布局**:
```
┌──────────────────────────────────────┐
│  同步状态                             │
│  ┌────────────────────────────┐      │
│  │ 最后同步: 2分钟前          │      │
│  │ 状态: ✅ 已同步            │      │
│  │ 待同步: 0项                │      │
│  │                            │      │
│  │ [立即同步] [查看详情]      │      │
│  └────────────────────────────┘      │
│                                       │
│  同步设置                             │
│  ┌────────────────────────────┐      │
│  │ 自动同步: [开启]           │      │
│  │ 同步间隔: [5分钟 ▼]        │      │
│  │ 仅WiFi同步: [✓]            │      │
│  │                            │      │
│  │ [保存设置]                 │      │
│  └────────────────────────────┘      │
└──────────────────────────────────────┘
```

**API接口**:
- `GET /api/v1/sync/status` - 获取同步状态
- `POST /api/v1/sync/pull` - 拉取云端数据
- `POST /api/v1/sync/push` - 推送本地数据
- `POST /api/v1/sync/resolve/:id` - 解决冲突

---

### 3.5 外观设置

**路由**: `/settings/appearance`

**功能**:
- 主题切换(亮色/暗色/自动)
- 字体大小
- 字体类型
- 编辑器主题

**界面元素**:
```tsx
<Form>
  <Form.Item label="主题">
    <Radio.Group>
      <Radio.Button value="light">☀️ 亮色</Radio.Button>
      <Radio.Button value="dark">🌙 暗色</Radio.Button>
      <Radio.Button value="auto">🔄 自动</Radio.Button>
    </Radio.Group>
  </Form.Item>
  <Form.Item label="字体大小">
    <Slider min={12} max={24} />
  </Form.Item>
  <Form.Item label="编辑器主题">
    <Select>
      <Option value="github">GitHub</Option>
      <Option value="monokai">Monokai</Option>
    </Select>
  </Form.Item>
</Form>
```

---

### 3.6 快捷键设置

**路由**: `/settings/shortcuts`

**功能**:
- 查看所有快捷键
- 自定义快捷键
- 重置为默认

**界面布局**:
```
┌─────────────────────────────────────┐
│  搜索快捷键...                       │
├─────────────────────────────────────┤
│  新建笔记        Ctrl + N   [编辑]  │
│  保存笔记        Ctrl + S   [编辑]  │
│  搜索笔记        Ctrl + F   [编辑]  │
│  切换主题        Ctrl + D   [编辑]  │
│  ────────────────────────────────  │
│  [重置为默认]                        │
└─────────────────────────────────────┘
```

---

### 3.7 关于

**路由**: `/settings/about`

**功能**:
- 应用版本信息
- 更新日志
- 开源协议
- 帮助文档链接
- 反馈渠道

---

## 四、路由结构

```typescript
// 路由配置
const settingsRoutes = [
  {
    path: '/settings',
    element: <SettingsLayout />,
    children: [
      { index: true, element: <Navigate to="/settings/account" /> },
      { path: 'account', element: <AccountSettings /> },
      { path: 'profile', element: <ProfileSettings /> },
      { path: 'ai-assistants', element: <AIAssistantsSettings /> },
      { path: 'models', element: <ModelsSettings /> },
      { path: 'sync', element: <SyncSettings /> },
      { path: 'appearance', element: <AppearanceSettings /> },
      { path: 'shortcuts', element: <ShortcutsSettings /> },
      { path: 'about', element: <AboutSettings /> }
    ]
  }
]
```

---

## 五、组件结构

```
src/pages/Settings/
├── SettingsPage.tsx          # 主页面
├── SettingsLayout.tsx        # 布局组件
├── SettingsMenu.tsx          # 左侧菜单
├── components/
│   ├── AccountSettings.tsx
│   ├── ProfileSettings.tsx
│   ├── AIAssistantsSettings.tsx
│   ├── ModelsSettings.tsx
│   ├── SyncSettings.tsx
│   ├── AppearanceSettings.tsx
│   ├── ShortcutsSettings.tsx
│   └── AboutSettings.tsx
└── hooks/
    └── useSettings.ts
```

---

## 六、状态管理

```typescript
// src/store/settingsStore.ts
interface SettingsStore {
  // 当前选中的菜单
  activeMenu: string
  setActiveMenu: (menu: string) => void

  // 用户设置
  userPreferences: {
    theme: 'light' | 'dark' | 'auto'
    fontSize: number
    fontFamily: string
    editorTheme: string
  }

  // 更新设置
  updatePreferences: (prefs: Partial<UserPreferences>) => Promise<void>
}
```

---

## 七、实施优先级

### P0 - 核心功能 (必须)
1. ✅ 基础设置页面布局
2. ✅ AI助手管理
3. ✅ 模型配置
4. ✅ 账号设置

### P1 - 重要功能 (应该)
5. ✅ 同步设置
6. ✅ 外观设置
7. ✅ 个人资料

### P2 - 增强功能 (可以)
8. ⏳ 快捷键设置
9. ⏳ 关于页面

---

## 八、API 对接清单

### AI助手管理
- [x] `GET /api/v1/ai/assistants` - 已实现
- [x] `POST /api/v1/ai/assistants` - 已实现
- [x] `PUT /api/v1/ai/assistants/:id` - 已实现
- [x] `DELETE /api/v1/ai/assistants/:id` - 已实现

### 模型配置
- [x] `GET /api/v1/models/configs` - 已实现
- [x] `POST /api/v1/models/configs` - 已实现
- [x] `PUT /api/v1/models/configs/:id` - 已实现
- [x] `DELETE /api/v1/models/configs/:id` - 已实现
- [ ] `GET /api/v1/models/usage` - 待实现
- [ ] `GET /api/v1/models/usage/logs` - 待实现

### 同步功能
- [x] `GET /api/v1/sync/status` - 已实现
- [x] `POST /api/v1/sync/pull` - 已实现
- [x] `POST /api/v1/sync/push` - 已实现
- [x] `POST /api/v1/sync/resolve/:id` - 已实现

### 用户管理
- [x] `GET /api/v1/users/me` - 已实现
- [x] `PUT /api/v1/users/me` - 已实现
- [ ] `PUT /api/v1/users/me/password` - 待实现

---

## 九、设计规范

### 9.1 颜色方案
```scss
// 设置页面专用颜色
$settings-bg: #f5f5f5;
$settings-sidebar-bg: #ffffff;
$settings-sidebar-hover: #f0f0f0;
$settings-sidebar-active: #e6f7ff;
$settings-border: #e8e8e8;
```

### 9.2 间距规范
- 侧边栏宽度: 240px
- 内容区域左右内边距: 24px
- 卡片间距: 16px
- 表单项间距: 24px

### 9.3 交互规范
- 点击菜单项后右侧内容区域切换
- 修改后显示"保存"和"取消"按钮
- 重要操作需要二次确认
- 成功/失败操作显示 Toast 提示

---

## 十、后续扩展

### 10.1 插件系统
未来可以在设置中添加插件管理模块

### 10.2 数据导入导出
支持数据备份和恢复功能

### 10.3 多设备管理
查看和管理已登录的设备

---

这个设计方案将所有需要管理数据库表的功能都整合到了统一的设置界面中,提供了清晰的用户体验和良好的可维护性。
