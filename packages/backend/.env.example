# Server
PORT=3001
HOST=0.0.0.0

# CORS
CORS_ORIGIN=http://localhost:3100

# Database
DATABASE_URL="postgresql://postgres:wangyh76@localhost:5432/ainote?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Encryption (for API keys) - 必须是64个十六进制字符(32字节)
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-character-hex-key-here-change-this-in-production

# Environment
NODE_ENV=development
